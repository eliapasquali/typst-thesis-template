#import "../config/thesis-config.typ": chapterHeading

#show: chapterHeading.with(type: "appendix")

#pagebreak(to: "odd")

= Appendice rimossa chirurgicamente

Qui is può inserire contenuto collegato alla tesi ma che non si inserisce bene all'interno del flusso principale.
